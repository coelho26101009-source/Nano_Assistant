model_name: "nano"
target_phrase:
  - "Nano"
custom_negative_phrases: []

n_samples: 20000
n_samples_val: 4000

tts_batch_size: 32
augmentation_batch_size: 64

piper_sample_generator_path: "REQUIRED BEFORE TRAINING"
output_dir: "models/wakeword/train-output"

rir_paths: []
background_paths: []
background_paths_duplication_rate: []
false_positive_validation_data_path: "REQUIRED BEFORE TRAINING"

augmentation_rounds: 2
feature_data_files: {}
batch_n_per_class: 32

model_type: "dnn"
layer_size: 32

steps: 20000
max_negative_weight: 1000
target_false_positives_per_hour: 0.5

notes:
  - "This is the openWakeWord 0.6.0-aligned Nano v1 configuration."
  - "Training is intentionally not started here."
  - "The final output will be versioned, for example: nano-v1.onnx."
